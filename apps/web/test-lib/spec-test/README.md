# @platform/spec-test (vendored)

Vendored copy of the spec-driven test runner from `elleskay/platform`. Do not edit in this repo. To update, rebuild in the platform repo (`cd packages/spec-test && npx tsc`) and copy `dist/` here.

Will be replaced with a published npm package later. See platform `packages/spec-test/` for source.
